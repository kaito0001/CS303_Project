import { StyleSheet } from 'react-native';

const HandleOrderStyle = StyleSheet.create({
    container: {
        marginHorizontal: '12%',
        flexDirection: 'row',
        justifyContent: 'space-between',
    },
    buttonTxT: {
        color: '#fff',
        fontWeight: '700',
        fontFamily: 'sans-serif',
    },
    button: {
        padding: 8,
        borderRadius: 15,
        backgroundColor: '#006cb7',
        alignItems: 'center',
    },
})

export default HandleOrderStyle;